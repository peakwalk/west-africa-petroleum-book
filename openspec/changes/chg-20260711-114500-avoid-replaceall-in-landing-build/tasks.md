## 1. Compatibility fix

- [x] 1.1 Add the OpenSpec change files for the landing `replaceAll` compatibility fix.
- [x] 1.2 Replace `replaceAll` in the landing outline-icon helper and add a regression assertion.

## 2. Verification

- [x] 2.1 Run `npm run build:site`.
- [x] 2.2 Start preview successfully and run `npm run test:site`.
- [x] 2.3 Validate the OpenSpec change.
