## 1. 兼容性修复

- [x] 1.1 为 landing `replaceAll` 兼容性修复补齐 OpenSpec 文件。
- [x] 1.2 替换 landing outline icon helper 里的 `replaceAll`，并增加回归断言。

## 2. 验证

- [x] 2.1 运行 `npm run build:site`
- [x] 2.2 成功启动 preview，并运行 `npm run test:site`
- [x] 2.3 校验 OpenSpec 变更
