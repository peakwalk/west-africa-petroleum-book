## 1. 共享分类

- [x] 1.1 新增共享页面变体模块，并在 shell 本地化阶段把计算出的 body class 直接写入生成后的书页。
- [x] 1.2 移除运行时页面变体分类器，并更新源码级测试和断言，改为覆盖构建期 body class 注入。

## 2. 运行时回归防护

- [x] 2.1 增加站点渲染校验，同时检查生成页面的 body class 与构建后的章节页运行时 outline 可见性，并在出现非预期空 outline 布局时失败。
- [x] 2.2 扩展自动化测试，覆盖该回归防护契约及新的共享脚本模块接入点。
- [x] 2.3 为本地验证增加一个基于 localhost 的可选 WKWebView 回放检查，并增强阅读器运行时启动兜底，确保回放时能够看到 hydration 后的 outline/reference 状态。

## 3. Figure 兜底增强

- [x] 3.1 增强运行时 figure caption 注释逻辑，支持 alt 标签与短相邻 caption 段落兜底。
- [x] 3.2 运行书籍/站点验证，并在任务状态中标记本 change 完成。
