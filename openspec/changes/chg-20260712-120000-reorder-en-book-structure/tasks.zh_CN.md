## 1. 英文源结构

- [x] 1.1 重排英文摘要；保留第5章在第二部分，并应用已确认的第二、三部分标题。
- [x] 1.2 将 10→7、7→8、8→9、9→10 映射应用到受影响英文章节标题、带编号标题和公式标识，不重命名章节文件。

## 2. 引用和生成站点输入

- [x] 2.1 更新英文手写目录以及图表、表格和公式索引，使标题和锚点与新编号映射和顺序一致。
- [x] 2.2 更新有意的英文正文引用和章节库部分标签映射；保持稳定路由映射及法文版不变。

## 3. 验证

- [x] 3.1 构建站点，检查生成的英文导航、章节元数据、手写索引链接和稳定章节路由。
- [x] 3.2 运行最小有用范围的英文 DOCX 一致性检查、站点回归套件，并按路径审阅最终差异，避免意外源文件或生成文件编辑。

## 4. 规范章节 URL 迁移

- [x] 4.1 重命名四个英文章节源文件，使其文件名匹配修订后的显示编号；随后更新全部英文源链接和测试引用。
- [x] 4.2 更新英法路由映射，并生成不进入站点地图的旧英文章节跳转页。
- [x] 4.3 构建站点，验证规范 URL、旧 URL 跳转、元数据、站点地图排除和相关回归检查。

## 5. 参考 DOCX 同步

- [x] 5.1 不使用 Microsoft Word，直接编辑英文参考 DOCX 的 OOXML 包，重排完整的第7–10章区块并应用确认后的编号映射。
- [x] 5.2 验证编辑后包结构，并运行英文 DOCX 一致性和公式覆盖检查。
- [x] 5.3 使用打包渲染器渲染同步后的 DOCX，检查代表性页面是否出现版式回归。
