## 1. 新增各 edition 的公式索引页

- [x] 1.1 在英文和法文 `SUMMARY.md` 中把 `List of Equations` / `Liste des équations` 插入到 `List of Tables` 之后。
- [x] 1.2 新增各 edition 的 `list-of-equations.md` 页面，只按章节收录已编号 equation，并链接到现有公式锚点。

## 2. 对齐 reader 页面变体与测试

- [x] 2.1 让 equation index 进入与 figure/table index 相同的 front-matter auxiliary index 页面类型。
- [x] 2.2 更新定向 site 与 metadata 断言，确保新页面在双语 edition 中都能构建、顺序正确并被暴露。
