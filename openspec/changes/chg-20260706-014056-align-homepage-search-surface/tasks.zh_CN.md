## 1. OpenSpec 与区块范围

- [x] 1.1 补齐首页搜索区块对齐改动的 proposal、design、spec 及中文配套文件。

## 2. 首页搜索区块实现

- [x] 2.1 更新首页内容生成逻辑，使搜索区块渲染为单个可见居中标题，以及不带独立提交按钮的一体化搜索框外壳。
- [x] 2.2 更新 landing 搜索区块样式和响应式覆盖规则，使标题、输入框宽度、图标位置、边框、圆角和间距贴近批准参考图，同时保留本地化文案和 chips。
- [x] 2.3 刷新与新搜索标记和 CSS 预期对应的首页验证断言。
- [x] 2.4 让 search-scope chips 对齐批准参考图的“图标前置”样式，包括每个 chip 的前置图标、更紧的 chip 间距，以及英文/法文首页里的本地化标签。
- [x] 2.5 把英文首页 `Browse by Topic` 的“大标题 + 10 卡片”结构替换成批准参考图里的 6 张 topic 卡片结构。
- [x] 2.6 为英文 topic reference 卡片补上区块本地的内联 SVG 图标集和更紧凑的文案层级，同时保留现有章节跳转目标。
- [x] 2.7 更新 landing 的 topic 区块样式和响应式覆盖规则，使标题、卡片密度、图标尺寸，以及法文兼容 fallback 行为贴近批准参考图。
- [x] 2.8 更新英文首页 `summary-modules` 的标记结构，让卡片补上批准参考图里的 CTA 链接，同时保留现有内容和内部跳转目标。
- [x] 2.9 调整 summary 卡片比例、标题排版、current-edition 强调方式，以及绿色勾选列表样式，使其贴近批准的底部参考图。

## 3. 重建与验证

- [x] 3.1 重建站点输出，让生成后的英文、法文和 public 首页变体都带上新的搜索区块。
- [x] 3.2 运行更新后首页搜索区块的针对性站点验证。
- [x] 3.3 扩展首页验证断言，覆盖新的英文 topic reference 区块，以及法文兼容 fallback 的保护约束。
- [x] 3.4 扩展首页验证断言，覆盖新的英文 `summary-modules` CTA 和卡片布局预期。
